# gh pr merge

```bash
gh pr merge 123 --squash --delete-branch
gh pr merge 123 --merge --auto
gh pr merge 123 --rebase --subject "feat: concise subject" --body "Merge via gh-cli automation"
```

用途：按指定策略合并 PR。
