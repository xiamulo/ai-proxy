name: 🔌 连接/创建模型失败（Trae 端）
description: Trae 中“添加模型失败/网络问题/创建失败”等连接相关问题
title: "[Conn] 简要描述"
labels: ["area:connection"]
body:
  - type: checkboxes
    id: precheck
    attributes:
      label: 预检查（请逐项确认）
      options:
        - label: hosts 包含 `127.0.0.1 api.openai.com` 且未被注释（# 开头）
          required: true
        - label: 已确认 443 端口未被其他进程占用
          required: true
        - label: API URL 只填域名（不包含 `/v1`）
          required: true
        - label: 已安装并信任本地 CA 证书（Windows 证书管理器 / macOS 钥匙串）
          required: true
  - type: dropdown
    id: platform
    attributes:
      label: 操作系统
      options: [Windows 10/11, macOS Apple Silicon, macOS Intel, Linux]
    validations:
      required: true
  - type: textarea
    id: sysinfo
    attributes:
      label: 系统与内核信息（务必粘贴命令输出）
      description: |
        - macOS / Linux：请粘贴 `uname -mprs` 的完整输出
        - Windows（PowerShell）：请粘贴以下命令的完整输出：
          `Get-CimInstance Win32_OperatingSystem | Select-Object Caption, Version, OSArchitecture`
      render: shell
    validations:
      required: true
  - type: input
    id: versions
    attributes:
      label: 版本信息
      placeholder: MTGA GUI vX.Y.Z / Trae vA.B.C
    validations:
      required: true
  - type: input
    id: apiurl
    attributes:
      label: 你的 API URL
      placeholder: https://your-api.example.com
    validations:
      required: true
  - type: input
    id: mapped_model
    attributes:
      label: Trae 中映射的模型 ID
      placeholder: 例如 gpt-4o / deepseek-chat / ...
  - type: textarea
    id: steps
    attributes:
      label: 复现步骤（务必具体）
      placeholder: |
        1. 打开 MTGA -> 一键启动
        2. Trae -> 添加模型 -> ...
    validations:
      required: true
  - type: textarea
    id: logs
    attributes:
      label: 相关日志与截图
      description: 建议包含 MTGA 控制台输出、Trae 弹窗、服务端日志（必要时用代码块）
      render: shell
